import { useEffect, useState } from "react"

const UseAdmin = email =>{
    const [isAdmin,setAdmin]=useState(false)
    useEffect(()=>{
        if(email){
            fetch(``)
        }
    },[email])
}